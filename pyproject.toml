[project]
name = "evergreen-multi-agents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"

[tool.uv.sources]
backend = { workspace = true }
pipeline = { workspace = true }

[tool.uv.workspace]
members = ["backend", "pipeline"]
exclude = []